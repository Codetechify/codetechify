name: Feature Backlog Workflow

on:
  issues:
    types: [opened, labeled]

jobs:
  manage-backlog:
    runs-on: ubuntu-latest
    if: github.event.label.name == 'feature' || (github.event.action == 'opened' && contains(github.event.issue.labels.*.name, 'feature'))

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install
        working-directory: .github/utils

      - name: Compile TypeScript
        run: npx tsc
        working-directory: .github/utils

      - name: Run script
        run: npm start
        env:
          CODETECHIFY_ACCESS_TOKEN: ${{ secrets.CODETECHIFY_ACCESS_TOKEN }}
          REPO_NAME: 'codetechify-repo'
          ORG_NAME: 'Codetechify'
          PROJECT_ID: '2'
        working-directory: .github/utils
